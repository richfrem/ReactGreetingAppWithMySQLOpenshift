apiVersion: v1
kind: Service
metadata:
  name: oracle-db
  labels:
    app: oracle-db
    app.kubernetes.io/name: oracle-db
    app.kubernetes.io/part-of: oracle-database
    app.openshift.io/runtime: database
  annotations:
    openshift.io/display-name: "Oracle Database Service"
spec:
  ports:
    - port: 1521
      targetPort: 1521
      protocol: TCP
      name: oracle
  selector:
    app: oracle-db
  type: ClusterIP
